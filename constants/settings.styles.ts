import { StyleSheet } from "react-native";

export const SETTINGS_STYLES = StyleSheet.create({
  settingsWrapper: {
    borderStyle: "solid",
    borderColor: "black",
    borderWidth: 1,
    borderRadius: 10,
  },
});
